{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit",
        "command": "which gcc 2>/dev/null && gcc --version 2>/dev/null || echo 'WARNING: GCC not found in PATH'"
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit",
        "command": "python -c \"import matplotlib; matplotlib.rcParams['font.sans-serif']\" 2>/dev/null || echo 'WARNING: Chinese font may not render'"
      }
    ]
  }
}
